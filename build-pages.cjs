const fs=require('fs');const s=fs.readFileSync('index.html','utf8');const headHeader=s.slice(0,s.indexOf('>',s.indexOf('<main'))+1);const tail=s.slice(s.indexOf('</main>'));function getSection(id){const idPos=s.indexOf('id="'+id+'"');const start=s.lastIndexOf('<section',idPos);const tok=/<section\b|<\/section>/g;tok.lastIndex=start;let depth=0,m;while(m=tok.exec(s)){if(m[0]==='</section>'){depth--;if(depth===0)return s.slice(start,tok.lastIndex)}else depth++}return ''}const pages={'index.html':'page-affiliate-hub','partner-benefits.html':'page-partner-benefits','instruction.html':'page-instruction','mob-cash.html':'page-mob-cash'};const navMap={'affiliate-hub':'/','partner-benefits':'/partner-benefits','instruction':'/instruction','mob-cash':'/mob-cash'};function fixNav(html){return html.replace(/<button class="nav-link( active)?" data-page="([^"]+)" onclick="showPage\('([^']+)'\)">([^<]*)<\/button>/g,function(mm,act,dp,pg,txt){return '<a class="nav-link'+(act||'')+'" href="'+navMap[pg]+'">'+txt+'</a>'}).replace(/onclick="showPage\('([^']+)'\);return false;"/g,function(mm,pg){return 'href="'+navMap[pg]+'"'}).replace(/<button class="btn ([^"]*)" onclick="showPage\('([^']+)'\)">([^<]*)<\/button>/g,function(mm,cls,pg,txt){return '<a class="btn '+cls+'" href="'+navMap[pg]+'">'+txt+'</a>'})}Object.keys(pages).forEach(function(file){let section=getSection(pages[file]);section=section.replace('class="page','class="page active');const out=fixNav(headHeader)+String.fromCharCode(10)+section+String.fromCharCode(10)+tail;fs.writeFileSync('dist-'+file,out);console.log(file,'section',section.length,'total',out.length)});